[project]
name = "parse_ioc"
version = "0.5.0"
description = "Parse a list of indicators into a dictionary or JSON structure for programmatic use."
readme = "README.md"
requires-python = ">=3.11"
dependencies = []
[project.urls]
Homepage = "https://github.com/bonifield/parse_ioc"
